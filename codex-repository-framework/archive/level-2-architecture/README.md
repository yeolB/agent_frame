# Archived Level 2 Architecture Extension

Status: frozen and non-operational.

이 디렉터리는 architecture governance까지 확장했던 이전 설계를 기록으로 보존합니다. Overlay, agent TOML, AGENTS additions는 현재 프레임의 일부가 아니며 자동 탐색 경로 밖에 있습니다.

다음을 하지 않습니다.

- `overlay/`를 프로젝트에 복사
- `AGENTS.additions.md`를 root instruction에 병합
- Coder, Steward, Architecture Reviewer 호출
- 구조 문제가 생겼다는 이유로 이 설계를 자동 활성화

사용자가 이 과거 설계의 검토나 부활을 명시적으로 요청한 경우에만 읽습니다. 그 경우에도 현재 요구에 맞는 새 설계 판단이 먼저이며, 이 archive를 그대로 설치하는 것을 기본값으로 삼지 않습니다.
